%h1 Listing authentications

%table
  %tr
    %th User
    %th Auth client
    %th Uid
    %th Token
    %th Secret
    %th Expires at
    %th
    %th
    %th

  - @authentications.each do |authentication|
    %tr
      %td= authentication.user_id
      %td= authentication.auth_client_id
      %td= authentication.uid
      %td= authentication.token
      %td= authentication.secret
      %td= authentication.expires_at
      %td= link_to 'Show', authentication
      %td= link_to 'Edit', edit_authentication_path(authentication)
      %td= link_to 'Destroy', authentication, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Authentication', new_authentication_path
